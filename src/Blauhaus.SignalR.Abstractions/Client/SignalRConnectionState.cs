namespace Blauhaus.SignalR.Abstractions.Client
{
    public enum SignalRConnectionState
    {
        Disconnected,
        Connected,
        Connecting,
        Reconnecting,
        Reconnected,
    }
}